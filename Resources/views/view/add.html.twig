{% extends 'EMSCoreBundle::base.html.twig' %}{% trans_default_domain 'EMSCoreBundle' %}

{% block title %}Add a new view for {{contentType.name}}{% endblock %}
{% block pagetitle %}Add a new view for {{contentType.name}}{% endblock %} 
{% block subtitle %}{% endblock %} 


{% block body %}

<div class="row">
	<div class="col-md-12">
		<!-- general form elements -->
		<div class="box box-primary">
			<div class="box-header with-border">
				<h3 class="box-title">New view for {{contentType.name}}</h3>
			</div>
			<!-- /.box-header -->
			<!-- form start -->
			{{ form_start(form) }}
				
				<div class="box-body">
					{{ form_row(form.name) }}
					{{ form_row(form.icon) }}
					{{ form_row(form.type) }}

				</div>
				<!-- /.box-body -->
				<div class="box-footer">
					{{ form_widget(form.create) }}
				</div>
			{{ form_end(form) }}

		</div>
		<!-- /.box -->
	</div>
</div>

{% endblock %} 


{% block javascripts %}
	{% include 'EMSCoreBundle:js:code-editor.html.twig' %}
	{% include 'EMSCoreBundle:app:menu.html.twig' with {
		'item':  'contenttype-index'
	}%}
{% endblock %}	