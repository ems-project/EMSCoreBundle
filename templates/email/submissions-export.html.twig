{% block body %}
    {% trans_default_domain 'emsco-core' -%}
    <!DOCTYPE html>
    <html>
    <head>
    </head>
    <body>
        <p>{{ 'email.crm_export.body_hello'|trans }}</p>
        <p>{{ 'email.crm_export.body_text'|trans }}</p>
        <p>{{ 'email.crm_export.body_greeting'|trans }}</p>
    </body>
    </html>
{% endblock %}
