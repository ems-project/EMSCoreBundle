{% trans_default_domain 'emsco-core' %}

{% block task_event_mail_subject %}
    {%- if type == 'approved' -%}
        {{ 'message.mail_approved_subject'|trans({ 'title': task.title, 'document': revision|emsco_display })|raw }}
    {%- elseif type == 'assignee_changed' -%}
        {{ 'message.mail_assignee_changed_subject'|trans({ 'title': task.title, 'document': revision|emsco_display })|raw }}
    {%- elseif type == 'completed' -%}
        {{ 'message.mail_completed_subject'|trans({ 'title': task.title, 'document': revision|emsco_display })|raw }}
    {%- elseif type == 'created' -%}
        {{ 'message.mail_created_subject'|trans({ 'title': task.title, 'document': revision|emsco_display })|raw }}
    {%- elseif type == 'deleted' -%}
        {{ 'message.mail_deleted_subject'|trans({ 'title': task.title, 'document': revision|emsco_display })|raw }}
    {%- elseif type == 'rejected' -%}
        {{ 'message.mail_rejected_subject'|trans({ 'title': task.title, 'document': revision|emsco_display })|raw }}
    {%- elseif type == 'updated' -%}
        {{ 'message.mail_updated_subject'|trans({ 'title': task.title, 'document': revision|emsco_display })|raw }}
    {%- endif -%}
{% endblock task_event_mail_subject %}

{% block task_event_mail_body %}
    <p>{{ 'field.from_sender'|trans({'sender': senderUsername|emsco_display_name}) }}</p>
    <p>{{ 'message.mail_greetings'|trans({'receiver': receiver.displayname}) }}</p>
    <p>{{ 'message.mail_task_has_been'|trans({'action': action, 'role': senderRole ? 'field.by_role'|trans({'role': senderRole}) : ''}) }}</p>
    <p>
        {% if task.hasDeadline %}<strong>{{ 'field.deadline'|trans }}:</strong>{{ task.deadline.format(date_format) }}<br/>{% endif %}
        <strong>{{ 'field.title'|trans }}:</strong> {{ task.title }}<br/>
        <strong>{{ 'field.assignee'|trans }}:&nbsp;</strong>{{ task.assignee|emsco_display_name }}<br/>
        <strong>{{ 'field.creator'|trans }}:&nbsp;</strong>{{ task.createdBy|emsco_display_name }}<br/>
        {% if task.hasDescription %}<strong>{{ 'field.description'|trans }}:</strong><br/>{{ task.description|nl2br }}{% endif %}
    </p>
    {% if comment %}<p><strong>{{ 'field.comment'|trans }}:</strong><br/>{{ comment|nl2br }}</p>{% endif %}
    <p>{{ 'key.link_to_document'|trans }}: <a href="{{ documentUrl }}" target="_blank"><strong>{{ revision|emsco_display }}</strong></a></p>
    <p>{{ 'message.mail_kind_regards'|trans }}</p>
{% endblock task_event_mail_body %}

{% block task_notification_mail_subject %}
    {{ subject|raw }}
{% endblock task_notification_mail_subject %}

{% block task_notification_mail_body %}
    <p>{{ 'message.mail_greetings'|trans({'receiver': receiver.displayname}) }}</p>

    <p>{{ 'message.mail_overdue_or_active_list'|trans }}</p>

    <ul>
        {% for revision in revisions[0:limit] %}
            {% set task = revision.taskCurrent %}
            <li>
                <p>
                    {{ task.status|trans }}: {{ task.title }} ({{ 'field.deadline'|trans }}: {{ task.deadline.format(date_format) }})<br/>
                    {{ 'key.link_to_document'|trans }}: <a href="{{ attribute(documentUrls, revision.ouuid) }}" target="_blank"><strong>{{ revision|emsco_display }}</strong></a>
                </p>
            </li>
        {% endfor %}
    </ul>

    <p>{{ 'message.mail_process_urgently'|trans }}</p>

    <p>{{ 'message.mail_kind_regards'|trans }}</p>
{% endblock task_notification_mail_body %}
